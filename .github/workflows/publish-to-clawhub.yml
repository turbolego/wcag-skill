name: Publish to ClawHub

# Manual-only: the reusable CLI-based publisher
# (openclaw/clawhub/.github/workflows/skill-publish.yml) currently fails
# with "Invalid publish output: 'pending-publication'" whenever a prior
# publish attempt for the same version is still undergoing security scan
# review; its status-key mapping doesn't recognize that state
# (see run 33077893243). publish-web.yml is the active push-triggered
# publisher until that upstream bug is fixed. Do not add a push trigger
# here at the same time as publish-web.yml — exactly one workflow may
# publish on push, or the two can race and double-publish the same commit.
on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: openclaw/clawhub/.github/workflows/skill-publish.yml@main
    permissions:
      contents: read
      id-token: write
    with:
      owner: turbolego
      skill_path: "."
      dry_run: false
    secrets:
      clawhub_token: ${{ secrets.CLAWHUB_TOKEN }}
